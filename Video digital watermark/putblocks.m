function mb=putblocks(b)
mb=zeros([16,16,3]);
%将前四个8x8数据块还原到mb(:,:,1)中
mb(1:8,1:8,1)=b(:,:,1);
mb(1:8,9:16,1)=b(:,:,2);
mb(9:16,1:8,1)=b(:,:,3);
mb(9:16,9:16,1)=b(:,:,4);
z=[1 1;1 1];
%对两个色度分量矩阵，分别将b(:,:,5)\b(:m,:,6)中的数据重复为2x2的小矩阵即可
mb(:,:,2)=kron(b(:,:,5),z);
mb(:,:,3)=kron(b(:,:,6),z);
end